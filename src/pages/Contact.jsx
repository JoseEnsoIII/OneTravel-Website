import { useEffect } from "react";
const Carousel = () => {
  useEffect(() => {
    document.title = "CONTACT";
  }, []);

  return (
    <section>
      
    </section>
  );
};

export default Carousel;